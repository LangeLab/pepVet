% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{batch_compare_enzymes}
\alias{batch_compare_enzymes}
\title{Compare multiple enzymes across a full proteome}
\usage{
batch_compare_enzymes(
  sequences,
  enzymes = c("trypsin", "lysc", "chymotrypsin-high", "asp-n endopeptidase",
    "arg-c proteinase"),
  cores = 1L,
  missed_cleavages = 1L,
  proteome = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{sequences}{Multi-protein input. Accepts the same forms as
\code{\link[=digest_protein]{digest_protein()}}. Must resolve to at least one protein. If \code{NULL} or
empty, raises an error.}

\item{enzymes}{Character vector of unique enzyme names to compare. Each name
must be one of pepVet's supported enzyme names. Defaults to a panel of five
commonly compared enzymes: trypsin, lysc, chymotrypsin-high,
asp-n endopeptidase, and arg-c proteinase.}

\item{cores}{Number of parallel workers passed to \code{\link[=batch_evaluate]{batch_evaluate()}} for
each enzyme. Proteins are split into \code{cores} equal chunks and processed
in parallel via \code{parallel::mclapply} (Unix only; silently serial on
Windows). Enzymes are always processed sequentially.}

\item{missed_cleavages}{Maximum missed cleavages passed to
\code{\link[=batch_evaluate]{batch_evaluate()}} for every enzyme. Defaults to \code{1L}.}

\item{proteome}{Optional proteome digest tibble passed to \code{\link[=batch_evaluate]{batch_evaluate()}}
for every enzyme. When \code{NULL} (default), no uniqueness scoring and the
\code{S_unique} column is omitted.}

\item{weights}{Optional scoring weight vector passed to \code{\link[=batch_evaluate]{batch_evaluate()}}.
When \code{NULL} (default), uses pepVet's default scoring weights.}

\item{...}{Additional scoring arguments passed to \code{\link[=batch_evaluate]{batch_evaluate()}}, such
as \code{gravy_range} and \code{length_range}.}
}
\value{
A tibble of class \code{pepvet_batch_comparison} with one row per
protein-enzyme pair. Columns: \code{protein_id}, \code{enzyme} (factor ordered
by the input \code{enzymes} vector, so ggplot2 axis and facet order matches
your specification), then all columns returned by \code{\link[=batch_evaluate]{batch_evaluate()}}:
\code{protein_length}, \code{n_peptides}, \code{n_valid_peptides}, component scores,
\code{composite_score}, \code{verdict}, \code{median_peptide_length}, and the four
difficulty flags. Printing shows a per-enzyme summary table before the
tibble rows.
}
\description{
\code{batch_compare_enzymes()} scores every protein in \code{sequences} against each
enzyme in \code{enzymes} and returns a tidy tibble with one row per
protein-enzyme pair. The input proteome is parsed once; each enzyme then
calls \code{\link[=batch_evaluate]{batch_evaluate()}} with \code{cores} workers that split proteins into
equal chunks processed via fork copy-on-write (\code{parallel::mclapply}).
This means \code{cores} workers are fully utilised regardless of how many enzymes
are compared, and the speedup applies equally to single-enzyme calls.
}
\section{Limitations}{

Enzymes run sequentially even when \code{cores > 1}; only proteins within each
enzyme are parallelized. Parallel execution requires Unix.
}

\examples{
small <- system.file(
  "extdata", "small_proteome_50_proteins.fasta",
  package = "pepVet"
)
result <- batch_compare_enzymes(small, enzymes = c("trypsin", "lysc"))
result
result[result$enzyme == "trypsin", c("protein_id", "composite_score")]
}
\seealso{
\code{\link[=batch_evaluate]{batch_evaluate()}}, \code{\link[=compare_digests]{compare_digests()}}, \code{\link[=summarize_batch]{summarize_batch()}}

Other evaluation:
\code{\link[=batch_evaluate]{batch_evaluate()}},
\code{\link[=compare_digests]{compare_digests()}},
\code{\link[=evaluate_digest]{evaluate_digest()}},
\code{\link[=recommend_enzyme]{recommend_enzyme()}},
\code{\link[=sensitivity_analysis]{sensitivity_analysis()}},
\code{\link[=summarize_batch]{summarize_batch()}},
\code{\link[=triage_proteins]{triage_proteins()}}
}
\concept{evaluation}
