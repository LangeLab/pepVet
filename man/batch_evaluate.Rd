% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{batch_evaluate}
\alias{batch_evaluate}
\title{Batch-evaluate multiple proteins}
\usage{
batch_evaluate(
  sequences,
  enzyme = "trypsin",
  missed_cleavages = 1L,
  include_cleavage_efficiency = FALSE,
  proteome = NULL,
  weights = NULL,
  cores = 1L,
  ...
)
}
\arguments{
\item{sequences}{Multi-protein input. Accepts the same forms as
\code{\link[=digest_protein]{digest_protein()}}. Must resolve to at least one protein. If \code{NULL} or
empty, raises an error.}

\item{enzyme}{Enzyme name passed to \code{\link[=digest_protein]{digest_protein()}}. Defaults to
\code{"trypsin"}.}

\item{missed_cleavages}{Maximum missed cleavages. Defaults to \code{1L}.}

\item{include_cleavage_efficiency}{Logical flag passed to
\code{\link[=digest_protein]{digest_protein()}}. Defaults to \code{FALSE}.
When \code{TRUE}, each per-protein peptide table includes a
\code{cleavage_efficiency}
column (does not affect the flat batch tibble columns).}

\item{proteome}{Optional proteome digest tibble passed to \code{\link[=score_peptides]{score_peptides()}}
for every protein evaluation. When \code{NULL} (default), no uniqueness
scoring is performed and the \code{S_unique} column is omitted.}

\item{weights}{Optional scoring weight vector passed to \code{\link[=score_peptides]{score_peptides()}}.
When \code{NULL} (default), uses pepVet's default scoring weights.}

\item{cores}{Number of parallel workers for protein-level chunking.
\code{1L} (default) runs sequentially with no extra dependencies. Values
greater than \code{1L} split the input into equal chunks and process each
chunk with \code{parallel::mclapply()} on Unix or a
\code{parallel::parLapply()} socket cluster on Windows.}

\item{...}{Additional scoring arguments passed to \code{\link[=score_peptides]{score_peptides()}}, such
as \code{gravy_range} and \code{length_range}.}
}
\value{
A tibble with one row per protein. Fixed columns:
\code{protein_id}, \code{protein_length}, \code{n_peptides}, \code{n_valid_peptides},
all available component scores (\code{S_length}, \code{S_coverage}, \code{S_count},
\code{S_hydro}, \code{S_charge}; \code{S_unique} when proteome is provided),
\code{composite_score}, \code{verdict}, \code{median_peptide_length},
\code{flag_short_protein}, \code{flag_hydrophobic}, \code{flag_low_complexity},
\code{flag_no_valid_peptides}.
}
\description{
\code{batch_evaluate()} processes proteins in bulk, using one digest and score
call per serial or parallel chunk, and returns a flat tibble with one row
per protein. Columns include \code{protein_id}, \code{protein_length}, and all
component scores, including \code{composite_score}, \code{verdict}, \code{n_peptides},
\code{n_valid_peptides}, and \code{median_peptide_length}, plus four
sequence-level difficulty flags. Pass the result to \code{\link[=summarize_batch]{summarize_batch()}} for
aggregate statistics or to \code{\link[=triage_proteins]{triage_proteins()}} for action labels.
}
\details{
The returned tibble carries a \code{scoring_config} attribute containing the
resolved ranges, weights, proteome-aware mode, and pI mode. Batch
inputs must have unique protein identifiers. Difficulty flags use the
active scoring ranges; \code{flag_short_protein} and \code{flag_low_complexity}
remain sequence-level heuristics. Rows follow the supplied input-record
order. Reordering uniquely named records changes row order but not the
named per-protein results.
}
\section{Limitations}{

Windows socket workers receive serialized copies of their input, while Unix
fork workers use copy-on-write memory. If a worker or socket cluster fails,
the affected chunks are retried sequentially with a warning.
}

\examples{
small_proteome <- system.file(
  "extdata", "small_proteome_50_proteins.fasta",
  package = "pepVet"
)
batch <- batch_evaluate(small_proteome, enzyme = "trypsin")
nrow(batch)
batch[, c("protein_id", "composite_score", "verdict")]
}
\seealso{
\code{\link[=evaluate_digest]{evaluate_digest()}}, \code{\link[=compare_digests]{compare_digests()}}, \code{\link[=summarize_batch]{summarize_batch()}},
\code{\link[=triage_proteins]{triage_proteins()}}

Other evaluation:
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}},
\code{\link[=compare_digests]{compare_digests()}},
\code{\link[=evaluate_digest]{evaluate_digest()}},
\code{\link[=recommend_enzyme]{recommend_enzyme()}},
\code{\link[=sensitivity_analysis]{sensitivity_analysis()}},
\code{\link[=summarize_batch]{summarize_batch()}},
\code{\link[=triage_proteins]{triage_proteins()}}
}
\concept{evaluation}
