% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{compare_digests}
\alias{compare_digests}
\title{Compare multiple enzymes on a single protein}
\usage{
compare_digests(
  sequence,
  enzymes = c("trypsin", "lysc"),
  missed_cleavages = 1L,
  proteome = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{sequence}{A single-protein input. Accepts the same forms as
\code{\link[=digest_protein]{digest_protein()}} but must resolve to exactly one protein. If \code{NULL} or
empty, raises an error.}

\item{enzymes}{Character vector of unique enzyme names to compare. Defaults
to \code{c("trypsin", "lysc")}. Each name must be one of pepVet's supported
cleaver-compatible enzyme names.}

\item{missed_cleavages}{Maximum missed cleavages passed to
\code{\link[=digest_protein]{digest_protein()}} for every enzyme. Defaults to \code{1L}.}

\item{proteome}{Optional proteome digest tibble passed to \code{\link[=score_peptides]{score_peptides()}}
for all enzyme evaluations. When \code{NULL} (default), no uniqueness scoring.}

\item{weights}{Optional scoring weight vector passed to \code{\link[=score_peptides]{score_peptides()}}.
When \code{NULL} (default), uses pepVet's default scoring weights.}

\item{...}{Additional arguments passed to \code{\link[=evaluate_digest]{evaluate_digest()}}. This includes
scoring arguments such as \code{gravy_range}, \code{length_range}, and
\code{include_pI}, plus \code{include_cleavage_efficiency} when peptide-level
cleavage annotations are requested during comparison.}
}
\value{
A tibble with one row per enzyme and columns \code{enzyme} followed by
the score columns returned by \code{\link[=evaluate_digest]{evaluate_digest()}}, sorted by
\code{composite_score} descending.
}
\description{
\code{compare_digests()} runs \code{\link[=evaluate_digest]{evaluate_digest()}} for each enzyme in \code{enzymes}
and returns a tibble of scores sorted by \code{composite_score} descending. Main
model-ranking function for pre-acquisition enzyme review.
}
\section{Limitations}{

Single-protein only. Use \code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}} for proteome-wide
enzyme comparison.
}

\examples{
bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
compare_digests(bsa_path, enzymes = c("trypsin", "lysc"))
}
\seealso{
\code{\link[=evaluate_digest]{evaluate_digest()}}, \code{\link[=recommend_enzyme]{recommend_enzyme()}}

Other evaluation:
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}},
\code{\link[=batch_evaluate]{batch_evaluate()}},
\code{\link[=evaluate_digest]{evaluate_digest()}},
\code{\link[=recommend_enzyme]{recommend_enzyme()}},
\code{\link[=sensitivity_analysis]{sensitivity_analysis()}},
\code{\link[=summarize_batch]{summarize_batch()}},
\code{\link[=triage_proteins]{triage_proteins()}}
}
\concept{evaluation}
