% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest.R
\name{digest_protein}
\alias{digest_protein}
\title{Simulate a proteolytic digest}
\usage{
digest_protein(
  sequence,
  enzyme = "trypsin",
  missed_cleavages = 1L,
  include_cleavage_efficiency = FALSE
)
}
\arguments{
\item{sequence}{Protein input supplied as a single sequence, a named
character vector of sequences, a \code{Biostrings::AAString}, a
\code{Biostrings::AAStringSet}, or a path to a FASTA file. File extension is
not used to detect FASTA input; any existing file that
\code{Biostrings::readAAStringSet()} can parse as FASTA is accepted. \code{NULL},
length-zero, and wrong-type inputs raise
\code{pepvet_error_invalid_input}; \code{NA} or an empty string raises
\code{pepvet_error_invalid_sequence}. Malformed FASTA files raise a classed
\code{pepvet_error_invalid_input} error.}

\item{enzyme}{Cleavage rule name. Defaults to \code{"trypsin"}. pepVet validates
this against its hard-coded registry of cleaver-compatible enzyme names,
including \code{trypsin}, \code{lysc}, \verb{glutamyl endopeptidase}, \verb{asp-n endopeptidase}, \code{chymotrypsin-high}, and \code{thermolysin}. Missing, empty,
wrong-type, and unsupported values raise \code{pepvet_error_invalid_enzyme}.}

\item{missed_cleavages}{Maximum number of missed cleavages to include.
Must be a single non-negative integer. Defaults to \code{1L}. Invalid values
raise \code{pepvet_error_invalid_missed_cleavages}.}

\item{include_cleavage_efficiency}{Logical flag indicating whether to append
a \code{cleavage_efficiency} column to the peptide output. Defaults to \code{FALSE}.
Trypsin-family digests receive sequence-local high/medium/low annotations;
unsupported enzymes return \code{NA} in this optional column. The flag must be
a single non-missing logical value; invalid values raise
\code{pepvet_error_invalid_include_cleavage_efficiency}.}
}
\value{
A tibble with one row per peptide and the columns \code{protein_id},
\code{peptide}, \code{start}, \code{end}, \code{length}, and \code{missed_cleavages}. Each row
represents one observed cleavage product for one protein under the
selected enzyme rule and missed-cleavage allowance. When
\code{include_cleavage_efficiency = TRUE}, the output also includes a
\code{cleavage_efficiency} column. The \code{cleavage_efficiency} values are all
\code{NA} for enzymes outside the trypsin family.
}
\description{
\code{digest_protein()} normalizes protein-like input, validates the amino-acid
alphabet, applies a cleaver-compatible enzyme rule, and returns peptide
coordinates in a tidy tibble. Entry point for all higher-level pepVet
workflows: scoring, enzyme comparison, and batch evaluation.
}
\details{
FASTA record names are preserved as \code{protein_id} values when they
are present, including irregular headers that do not use UniProt pipe
formatting. Unnamed input sequences receive generated \verb{sequence_<n>} IDs.
pepVet uses cleaver-compatible cleavage rules for the strict cut sites and
expands missed cleavages itself so repeated peptides and overlapping ranges
retain exact start and end coordinates. Peptides are returned whether or
not they later count as valid in the pepVet scoring model. Validity is a
separate decision controlled by \code{score_peptides()} through \code{length_range}.

When cleavage-efficiency annotations are requested, pepVet records the
weakest annotated efficiency class across cleavage sites that delimit or
fall within a peptide. These annotations reflect local P1-P1' sequence
context only. They do not model extended subsite preferences, structural
protection, or PTMs that alter cleavage behavior.
}
\section{Limitations}{
 Cleaver rules only, no structural accessibility,
no PTM awareness.
}

\examples{
digest_protein("MKWVTFISLLFLFSSAYSR")
digest_protein(Biostrings::AAString("MKWVTFISLLFLFSSAYSR"))
digest_protein("AKRTPK", include_cleavage_efficiency = TRUE)
}
\seealso{
Other digest:
\code{\link[=annotate_cleavage_sites]{annotate_cleavage_sites()}}
}
\concept{digest}
