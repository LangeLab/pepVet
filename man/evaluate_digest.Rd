% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{evaluate_digest}
\alias{evaluate_digest}
\title{Evaluate a Proteolytic Digest}
\usage{
evaluate_digest(
  sequence,
  enzyme = "trypsin",
  missed_cleavages = 0L,
  proteome = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{sequence}{Protein input. Accepts the same forms as \code{\link[=digest_protein]{digest_protein()}}:
a character sequence, named character vector, \code{Biostrings::AAString},
\code{Biostrings::AAStringSet}, or a FASTA file path.}

\item{enzyme}{Enzyme name passed to \code{\link[=digest_protein]{digest_protein()}}. Defaults to
\code{"trypsin"}.}

\item{missed_cleavages}{Maximum missed cleavages passed to
\code{\link[=digest_protein]{digest_protein()}}. Defaults to \code{0L}.}

\item{proteome}{Optional proteome digest tibble passed to \code{\link[=score_peptides]{score_peptides()}}
for peptide uniqueness scoring.}

\item{weights}{Optional scoring weight vector passed to \code{\link[=score_peptides]{score_peptides()}}.
When scoring a non-tryptic digest directly, \code{\link[=evaluate_digest]{evaluate_digest()}} forwards
the selected \code{enzyme} so enzyme-aware S_count denominators stay aligned
with the digest.}

\item{...}{Additional scoring arguments passed to \code{\link[=score_peptides]{score_peptides()}}, such
as \code{gravy_range} and \code{length_range}. This makes workflow presets from
\code{\link[=pepvet_preset]{pepvet_preset()}} directly compatible with \code{\link[=evaluate_digest]{evaluate_digest()}} through
\code{do.call()} or argument splicing.}
}
\value{
A named list with three elements:
\describe{
\item{\code{scores}}{A tibble from \code{\link[=score_peptides]{score_peptides()}} with one row per
protein.}
\item{\code{peptides}}{A tibble from \code{\link[=digest_protein]{digest_protein()}} with one row per
peptide.}
\item{\code{params}}{A list recording the resolved \code{enzyme} name,
\code{missed_cleavages} count, \code{protein_ids} found in the input, and the
resolved \code{preset_used} label from \code{\link[=score_peptides]{score_peptides()}}.}
}
}
\description{
\code{evaluate_digest()} combines \code{\link[=digest_protein]{digest_protein()}} and \code{\link[=score_peptides]{score_peptides()}} into
a single call and returns a named list containing the peptide table, the
score table, and the resolved input parameters. Use it when you want a full
digest object for one protein and one enzyme without manually wiring the two
lower-level functions together.
}
\details{
\code{evaluate_digest()} preserves pepVet's scoring metadata so the
returned object can be interpreted honestly outside the immediate scoring
call. In particular, \code{params$preset_used} records whether the resolved
scoring configuration matches one of pepVet's named presets or should be
treated as \code{"custom"}.
}
\examples{
bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
result <- evaluate_digest(bsa_path, enzyme = "trypsin")
result$scores
result$params$enzyme
result$params$preset_used
}
\seealso{
\code{\link[=digest_protein]{digest_protein()}}, \code{\link[=score_peptides]{score_peptides()}}, \code{\link[=compare_digests]{compare_digests()}},
\code{\link[=batch_evaluate]{batch_evaluate()}}
}
