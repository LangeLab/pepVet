% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R
\name{pepvet_plot_config}
\alias{pepvet_plot_config}
\title{Configure pepVet plot appearance}
\usage{
pepvet_plot_config(palette = NULL, params = NULL, theme = NULL)
}
\arguments{
\item{palette}{Named list of color overrides. Names must match existing
\code{.pepvet_pal} entries (e.g. \code{list(brand = "#004488", good = "#2ECC71")}).
Sub-lists like \code{verdict}, \code{component}, and \code{overlap} are replaced entirely.}

\item{params}{Named list of parameter overrides. Names must match existing
\code{.pepvet_params} entries (e.g. \code{list(verdict_good = 0.70, scatter_alpha = 0.90)}).}

\item{theme}{Named list of ggplot2 theme element overrides. Passed directly
to \code{\link[ggplot2:theme]{ggplot2::theme()}} and applied on top of the base \code{.pepvet_theme()}.
(e.g. \code{list(legend.position = "right", plot.title = element_text(size = 14))}).}
}
\value{
Invisibly returns a list with current \code{palette}, \code{params}, and \code{theme}.
}
\description{
Override default colors, numeric parameters, and theme elements used by
all pepVet plotting functions. Changes persist for the session.
Call with no arguments to view the current configuration.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  # View current config
  pepvet_plot_config()

  # Customize brand color and Good threshold
  pepvet_plot_config(
    palette = list(brand = "#004488", good = "#2ECC71"),
    params  = list(verdict_good = 0.70)
  )

  # Reset to defaults
  pepvet_plot_config_reset()
}
}
\seealso{
Other plot-utils:
\code{\link[=pepvet_plot_config_reset]{pepvet_plot_config_reset()}},
\code{\link[=pepvet_save_figure]{pepvet_save_figure()}},
\code{\link[=pepvet_theme_manuscript]{pepvet_theme_manuscript()}},
\code{\link[=pepvet_theme_presentation]{pepvet_theme_presentation()}}
}
\concept{plot-utils}
