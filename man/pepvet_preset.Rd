% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pepvet_preset}
\alias{pepvet_preset}
\title{Return a named scoring preset}
\usage{
pepvet_preset(type = "standard")
}
\arguments{
\item{type}{Preset name. Defaults to \code{"standard"}. Supported values are
\code{"standard"}, \code{"dia"}, \code{"targeted"}, \code{"membrane"}, \code{"ffpe_degraded"}, and
\code{"fractionated"}. If \code{NULL}, raises an error.}
}
\value{
A named list with \code{gravy_range}, \code{length_range}, \code{weights}, and
\code{include_pI}.
The returned object can be passed directly into \code{\link[=score_peptides]{score_peptides()}} or
\code{\link[=evaluate_digest]{evaluate_digest()}} through \code{do.call()} or argument splicing.
}
\description{
\code{pepvet_preset()} returns a named list containing a GRAVY range, peptide
length range, and scoring weights for a supported proteomics workflow.
Presets are intended as editable starting points rather than hard rules.
Their exact ranges and weights are conservative package choices, not
empirically calibrated boundaries.
}
\section{Presets}{


\strong{\code{"standard"}} : Default scoring configuration for routine DDA examples.
Uses the \verb{[7, 25]} length range, \verb{[-1.0, 0.6]} GRAVY range, and default
protein-only weights.

\strong{\code{"dia"}} : A wider starting configuration for DIA or SWATH examples.
Uses the \verb{[7, 30]} length range, \verb{[-1.0, 0.8]} GRAVY range, and a larger
coverage weight than the standard preset.

\strong{\code{"targeted"}} : A narrower starting configuration for SRM, PRM, or MRM
examples. Uses the \verb{[8, 20]} length range and \verb{[-0.8, 0.4]} GRAVY range.
When a background proteome digest is supplied, uniqueness receives 30 percent
of the composite weight.

\strong{\code{"membrane"}} : A wider hydrophobicity configuration for membrane-protein
review. Uses the \verb{[7, 30]} length range, extends the upper GRAVY boundary to
\code{2.0}, and assigns 5 percent of the composite weight to \code{S_hydro}.

\strong{\code{"ffpe_degraded"}} : A broader length configuration for exploratory work
with degraded or FFPE-derived material. Uses the \verb{[6, 30]} length range and
assigns more weight to \code{S_count} than the standard preset.

\strong{\code{"fractionated"}} : SCX / high-pH RP fractionation planning. Same
scoring parameters as \code{"standard"} but with \code{include_pI = TRUE}
to append peptide-level pI values for fractionation-aware analysis.
}

\section{Limitations}{

The six presets encode package priors. Inspect the returned values and use
explicit arguments when the experimental context calls for other choices.
}

\examples{
pepvet_preset("standard")
}
\seealso{
Other utils:
\code{\link[=calculate_pI]{calculate_pI()}},
\code{\link[=calculate_peptide_mass]{calculate_peptide_mass()}}
}
\concept{utils}
