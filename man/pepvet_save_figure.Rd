% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R
\name{pepvet_save_figure}
\alias{pepvet_save_figure}
\title{Save a pepVet figure with publication-ready defaults}
\usage{
pepvet_save_figure(
  plot,
  filename = "pepvet_plot.png",
  width = NULL,
  height = NULL,
  dpi = 300,
  bg = "white",
  device = NULL,
  ...
)
}
\arguments{
\item{plot}{A ggplot or patchwork object produced by any pepVet plot function.}

\item{filename}{Character path for the output file. Extensions \code{.png}, \code{.pdf},
\code{.svg}, etc. are handled by \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. Defaults to \code{"pepvet_plot.png"}
in the working directory.}

\item{width, height}{Numeric. Plot dimensions in inches. When \code{NULL} (default),
auto-sized: single-panel = 10x7, multi-panel patchwork = 14x10.}

\item{dpi}{Numeric. Resolution in dots per inch. Defaults to \code{300} (publication).}

\item{bg}{Character. Background color. Defaults to \code{"white"}.}

\item{device}{Device to use. When \code{NULL} (default) and the filename extension
is \code{.png}, tries \code{\link[ragg:agg_png]{ragg::agg_png()}} for anti-aliased output, falling back
to \code{"png"}. For other extensions, the device is inferred from the filename.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The saved file path invisibly.
}
\description{
Wraps \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with pepVet's recommended defaults: auto-sizing
based on whether the plot is a multi-panel patchwork or a single panel,
300 DPI, anti-aliased PNG via ragg when available, and white background.
All arguments in \code{...} are passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} and can override
the defaults.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE)) {
  bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
  res <- evaluate_digest(bsa_path, enzyme = "trypsin")
  p <- plot_digest_profile(res)
  tmp <- tempfile(fileext = ".png")
  pepvet_save_figure(p, tmp)
}
}
\seealso{
Other plot-utils:
\code{\link[=pepvet_plot_config]{pepvet_plot_config()}},
\code{\link[=pepvet_plot_config_reset]{pepvet_plot_config_reset()}},
\code{\link[=pepvet_theme_manuscript]{pepvet_theme_manuscript()}},
\code{\link[=pepvet_theme_presentation]{pepvet_theme_presentation()}}
}
\concept{plot-utils}
