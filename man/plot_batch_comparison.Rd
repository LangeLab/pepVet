% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_batch.R
\name{plot_batch_comparison}
\alias{plot_batch_comparison}
\title{Multi-enzyme proteome comparison}
\usage{
plot_batch_comparison(comparison, title = NULL)
}
\arguments{
\item{comparison}{A \code{pepvet_batch_comparison} tibble returned by
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}}, with columns \code{protein_id}, \code{enzyme},
\code{composite_score}, \code{verdict}, and the five component score columns.  A
proteome-aware comparison may also contain \code{S_unique}, which is included
in the component heatmap.  If \code{NULL} or empty, raises an error.}

\item{title}{Optional character title for the combined figure. When \code{NULL}
(default), generates an auto-title with protein and enzyme counts.}
}
\value{
A \code{patchwork} object with four panels: verdict summary bars (A),
score distribution violins (B), component-score heatmap (C), and
per-protein win-rate bars (D).
}
\description{
\code{plot_batch_comparison()} produces a four-panel side-by-side comparison of
enzyme performance across a full proteome, using output from
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}}:
}
\details{
\itemize{
\item \strong{(A) Verdict summary:} 100\% stacked horizontal bars showing the
Good/Moderate/Poor verdict breakdown per enzyme.  Enzymes are ordered
by descending Good\%, and the highest model summary is marked with a star.
\item \strong{(B) Score distributions:} horizontal violin plots for each enzyme,
showing the full distribution of composite scores.  An IQR boxplot is
overlaid on each violin.  Violin fill color reflects the enzyme's median
verdict.
\item \strong{(C) Component heatmap:} median component scores in an
enzyme-by-component
grid, filled by the verdict gradient (red to amber to green). Reveals which
digest quality dimension differentiates the enzymes.
\item \strong{(D) Per-protein win rate:} bar chart showing the proportion of proteins
for which each enzyme achieves the highest composite score. The starred
enzyme matches the top model summary in panel A.
}
}
\section{Limitations}{

At proteome sizes exceeding 2000 proteins the figure panels become dense
and text labels may overlap. Consider filtering the comparison to a
representative subset or increasing the output dimensions.
}

\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
  requireNamespace("patchwork", quietly = TRUE)) {
  small <- system.file(
    "extdata", "small_proteome_50_proteins.fasta",
    package = "pepVet"
  )
  comp <- batch_compare_enzymes(small, enzymes = c("trypsin", "lysc"))
  plot_batch_comparison(comp)
}
}
\seealso{
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}} for the upstream comparison step.

Other plot-batch:
\code{\link[=plot_proteome_overview]{plot_proteome_overview()}}
}
\concept{plot-batch}
