% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_single.R
\name{plot_digest_profile}
\alias{plot_digest_profile}
\title{Four-Panel Digest Diagnostic Plot}
\usage{
plot_digest_profile(
  result,
  length_range = c(7L, 25L),
  gravy_range = c(-1, 0.6),
  title = NULL
)
}
\arguments{
\item{result}{A named list returned by \code{\link[=evaluate_digest]{evaluate_digest()}}.  Must describe
a single protein (one unique \code{protein_id} in \code{result$peptides}).}

\item{length_range}{Integer vector of length 2.  Defines the valid peptide
length window, passed to the length and coverage panels.  Defaults to
\code{c(7L, 25L)}.}

\item{gravy_range}{Numeric vector of length 2.  Defines the LC-friendly
GRAVY range shaded in panel B.  Defaults to \code{c(-1.0, 0.6)}.}

\item{title}{Optional character string for the figure title.  When \code{NULL}
(default) a title is auto-generated from the protein accession and enzyme.}
}
\value{
A \code{patchwork} object combining all four ggplot panels.  The object
can be printed directly, saved with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, or composed
further with other patchwork operators.
}
\description{
\code{plot_digest_profile()} assembles a four-panel figure for a single
protein–enzyme pair from an \code{\link[=evaluate_digest]{evaluate_digest()}} result.  The panels are:
}
\details{
\itemize{
\item \strong{(A) Length distribution} — histogram of peptide lengths with the valid
window shaded.  Bars are colored by length class: valid (blue), too short
(amber), too long (rose).
\item \strong{(B) GRAVY distribution} — histogram of GRAVY hydrophobicity scores.
The LC-friendly range is shaded and bounded by dashed lines.
\item \strong{(C) Coverage map} — protein drawn as a horizontal track with
valid-length peptides overlaid as colored segments.  Uncovered regions
are highlighted in red.  Peptide length labels appear inside segments of
8 aa or longer.
\item \strong{(D) Component scores} — horizontal bar chart for each scoring
component, colored by tier (green \eqn{\geq} 0.70, amber 0.40–0.69, red
< 0.40).  The composite score is marked with a dashed vertical line.
}

GRAVY scores are computed internally from the peptide sequences in
\code{result$peptides} using the Kyte–Doolittle scale.  No external columns are
required beyond the standard \code{\link[=evaluate_digest]{evaluate_digest()}} output.

Panel C labels peptide lengths inside segments of 8 aa or longer.  For
heavily digested proteins this keeps the map readable without overlap.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE)) {
  bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
  res <- evaluate_digest(bsa_path, enzyme = "trypsin")
  p   <- plot_digest_profile(res)
  print(p)
}

}
\seealso{
\code{\link[=evaluate_digest]{evaluate_digest()}}, \code{\link[=plot_coverage_map]{plot_coverage_map()}},
\code{\link[=plot_enzyme_comparison]{plot_enzyme_comparison()}}
}
