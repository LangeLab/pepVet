% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_comparison.R
\name{plot_enzyme_protein_heatmap}
\alias{plot_enzyme_protein_heatmap}
\title{Enzyme × Protein Score Heatmap}
\usage{
plot_enzyme_protein_heatmap(
  results,
  component = "composite_score",
  show_verdict = TRUE,
  title = NULL
)
}
\arguments{
\item{results}{A named list of named lists.  Outer names = protein labels,
inner names = enzyme labels, each leaf = an \code{\link[=evaluate_digest]{evaluate_digest()}} result.
\strong{Or} a long-format data.frame / tibble with columns \code{protein_label},
\code{enzyme}, and all component score columns (as returned by iterating
\code{\link[=batch_evaluate]{batch_evaluate()}} across multiple enzymes and binding rows with an
\code{enzyme} column).}

\item{component}{Character.  Which score to use as the fill.  One of
\code{c("composite_score","S_length","S_coverage","S_count","S_hydro","S_charge")}.
Defaults to \code{"composite_score"}.}

\item{show_verdict}{Logical.  When \code{TRUE} (default) prints a one-letter
verdict badge (G/M/P) inside each tile.}

\item{title}{Optional character title.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_enzyme_protein_heatmap()} draws a 2D tile matrix: rows = proteins
(y-axis), columns = enzymes (x-axis), fill = score.  It is the only
pepVet function that shows the full multi-protein × multi-enzyme comparison
space simultaneously.
}
