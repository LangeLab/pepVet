% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_single.R
\name{plot_peptide_overlap_map}
\alias{plot_peptide_overlap_map}
\title{Amino-Acid Peptide Overlap Map}
\usage{
plot_peptide_overlap_map(
  result,
  length_range = c(7L, 25L),
  missed_cleavages = 1L,
  residues_per_line = 50L,
  title = NULL
)
}
\arguments{
\item{result}{A named list returned by \code{\link[=evaluate_digest]{evaluate_digest()}}. Must describe a
single protein.}

\item{length_range}{Optional integer vector of length 2 defining which
peptide lengths count toward overlap. Defaults to \code{c(7L, 25L)}. Peptides
are counted from the MC level specified by \code{missed_cleavages}. Use
\code{length_range = NULL} to count all digested peptides at all
missed-cleavage levels.}

\item{missed_cleavages}{Integer. Which missed-cleavage level to use for
the overlap count. Defaults to \code{1L} (standard bottom-up practice).
Pass \code{0L} for strict non-overlapping fragments, or \code{NULL} to count
all MC levels (automatically set when \code{length_range = NULL}).}

\item{residues_per_line}{Positive integer. Number of residues to show before
wrapping to the next display row. Defaults to \code{50L}.}

\item{title}{Optional character string for the plot title. Auto-generated
from the protein accession and enzyme when \code{NULL} (default).}
}
\value{
A \code{ggplot} object showing one tile per residue.
}
\description{
\code{plot_peptide_overlap_map()} renders a wrapped residue-level view of a
single protein and colors each amino-acid tile by how many peptides
cover that residue (MC=0 only by default). Uncovered residues are white;
increasing overlap depth gets progressively stronger blue fills.
The subtitle reports the maximum overlap, which helps gauge whether the
6-color scale (0, 1, 2-3, 4-7, 8-15, 16+) is sufficient for your data.
}
\details{
The plotted letters are reconstructed from the MC=0 peptide set in
\code{result$peptides}, so the plot can be generated directly from an
\code{\link[=evaluate_digest]{evaluate_digest()}} result without re-reading the original FASTA input.
Overlap counts are computed from valid-length MC=1 peptides by default
(configurable via \code{missed_cleavages}). Pass \code{missed_cleavages = 0L} for
strict non-overlapping fragments, or \code{length_range = NULL} to count all
digested peptides at all missed-cleavage levels.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
  res <- evaluate_digest(bsa_path, enzyme = "trypsin", missed_cleavages = 1)
  p <- plot_peptide_overlap_map(res)
  print(p)
}

}
\seealso{
\code{\link[=evaluate_digest]{evaluate_digest()}}, \code{\link[=plot_coverage_map]{plot_coverage_map()}}, \code{\link[=plot_cleavage_map]{plot_cleavage_map()}}

Other plot-single:
\code{\link[=plot_cleavage_map]{plot_cleavage_map()}},
\code{\link[=plot_coverage_map]{plot_coverage_map()}},
\code{\link[=plot_digest_profile]{plot_digest_profile()}},
\code{\link[=plot_weight_sensitivity]{plot_weight_sensitivity()}}
}
\concept{plot-single}
