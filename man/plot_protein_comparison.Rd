% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_protein_comparison}
\alias{plot_protein_comparison}
\title{Protein Comparison — Component Scores Across Multiple Proteins}
\usage{
plot_protein_comparison(
  results,
  components = c("S_length", "S_coverage", "S_count", "S_hydro", "S_charge",
    "composite_score"),
  show_verdict = TRUE,
  title = NULL
)
}
\arguments{
\item{results}{A named list of \code{\link[=evaluate_digest]{evaluate_digest()}} results (each for a
different protein, same enzyme), \strong{or} a \code{batch_evaluate()} tibble.
List names are used as protein labels; when unnamed the function falls
back to protein IDs extracted from \code{result$params}.}

\item{components}{Character vector of component score column names to show.
Defaults to all standard components (\code{S_length}, \code{S_coverage}, \code{S_count},
\code{S_hydro}, \code{S_charge}) plus \code{composite_score}.}

\item{show_verdict}{Logical.  When \code{TRUE} (default) a verdict badge is
drawn above each protein's group.}

\item{title}{Optional character string for the plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_protein_comparison()} draws a grouped bar chart of component scores
for multiple proteins digested with the same enzyme, allowing direct
side-by-side comparison.  It is the multi-protein mirror of
\code{\link[=plot_enzyme_comparison]{plot_enzyme_comparison()}}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
  h3_path  <- system.file("extdata", "P68431.fasta", package = "pepVet")
  results  <- list(
    BSA = evaluate_digest(bsa_path, enzyme = "trypsin"),
    H3  = evaluate_digest(h3_path,  enzyme = "trypsin")
  )
  p <- plot_protein_comparison(results)
  print(p)
}

}
\seealso{
\code{\link[=plot_enzyme_comparison]{plot_enzyme_comparison()}}, \code{\link[=evaluate_digest]{evaluate_digest()}}, \code{\link[=batch_evaluate]{batch_evaluate()}}
}
