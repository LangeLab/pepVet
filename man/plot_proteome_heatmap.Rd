% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_batch.R
\name{plot_proteome_heatmap}
\alias{plot_proteome_heatmap}
\title{Proteome Component Score Heatmap}
\usage{
plot_proteome_heatmap(
  batch,
  components = c("S_length", "S_coverage", "S_count", "S_hydro", "S_charge"),
  cluster_rows = TRUE,
  cluster_cols = FALSE,
  title = NULL
)
}
\arguments{
\item{batch}{A tibble from \code{\link[=batch_evaluate]{batch_evaluate()}}, with columns \code{protein_id},
\code{verdict}, and the component score columns.}

\item{components}{Character vector of component columns to include.
Defaults to \code{c("S_length","S_coverage","S_count","S_hydro","S_charge")}.}

\item{cluster_rows}{Logical.  Cluster rows (proteins) by score similarity.
Default \code{TRUE}.}

\item{cluster_cols}{Logical.  Cluster columns (components).  Default \code{FALSE}
(keeps the natural component order).}

\item{title}{Optional character title passed to \code{pheatmap::pheatmap()}.}
}
\value{
A \code{pheatmap} object (not a ggplot).
}
\description{
\code{plot_proteome_heatmap()} draws a clustered heatmap of all component scores
across all proteins in a \code{\link[=batch_evaluate]{batch_evaluate()}} result.  Rows (proteins) are
hierarchically clustered by score profile similarity so proteins that fail
for the same reason appear together.  A verdict color sidebar is drawn on
the left.
}
\details{
Requires the \code{pheatmap} package (suggested dependency).
}
