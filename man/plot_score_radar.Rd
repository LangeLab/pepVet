% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_comparison.R
\name{plot_score_radar}
\alias{plot_score_radar}
\title{Score Radar (Spider) Chart}
\usage{
plot_score_radar(
  comparison,
  scores = c("S_coverage", "S_length", "S_count", "S_hydro", "S_charge"),
  title = NULL,
  legend_ncol = NULL,
  legend_nrow = NULL
)
}
\arguments{
\item{comparison}{A tibble returned by \code{\link[=compare_digests]{compare_digests()}}, \strong{or} a named
list returned by \code{\link[=evaluate_digest]{evaluate_digest()}} (single-enzyme radar).  When a
single \code{evaluate_digest()} result is supplied the polygon is labeled with
the enzyme name stored in \code{result$params$enzyme}.}

\item{scores}{Character vector of component-score column names to use as
radar axes.  Any column absent from \code{comparison} is silently dropped.
Defaults to all five standard component scores.}

\item{title}{Optional character string for the plot title.  Auto-generated
when \code{NULL} (default).}

\item{legend_ncol}{Integer.  Number of columns in the enzyme legend.
Defaults to \code{min(n_enzymes, 4)} so up to four enzymes sit in one row and
larger sets wrap onto additional rows automatically.}

\item{legend_nrow}{Integer.  Number of rows in the enzyme legend.  Leave as
\code{NULL} (default) to let \code{legend_ncol} control wrapping.}
}
\value{
A \code{ggplot} object that can be printed, further customised, or saved
with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
\code{plot_score_radar()} draws a radar / spider chart of component scores for
one or more enzymes, overlaid as filled polygons on a regular grid.  The
polygon \emph{shape} reveals tradeoffs that bar charts hide: an enzyme with high
coverage but low peptide count produces a very different silhouette from one
that scores evenly across all components.
}
\details{
The chart is built from pure ggplot2 Cartesian geometry — no additional
packages beyond those already in \code{Suggests} are required.  Axes radiate
from the centre at equal angular intervals, starting from the top and
proceeding clockwise.  Concentric reference polygons at 0.25, 0.50, 0.75,
and 1.00 act as a grid.

Axis labels use short human-readable names (e.g. "Coverage",
"Length").  The label for each axis is placed just outside the outer ring
(radius 1.18) and auto-justified toward the centre so it does not clip
against the panel border.

When more than five enzymes are compared, enzyme colors cycle through the
eight-color JCO-inspired palette.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  bsa_path <- system.file("extdata", "P02769.fasta", package = "pepVet")
  comp <- compare_digests(bsa_path,
                          enzymes = c("trypsin", "lysc",
                                      "glutamyl endopeptidase"))
  p <- plot_score_radar(comp)
  print(p)
}

}
\seealso{
\code{\link[=compare_digests]{compare_digests()}}, \code{\link[=plot_enzyme_comparison]{plot_enzyme_comparison()}},
\code{\link[=plot_digest_profile]{plot_digest_profile()}}
}
