% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{score_peptides}
\alias{score_peptides}
\title{Score digested peptides for proteomics suitability}
\usage{
score_peptides(
  digest_result,
  proteome = NULL,
  weights = NULL,
  gravy_range = c(-1, 0.6),
  length_range = c(7L, 25L),
  enzyme = "trypsin",
  include_pI = FALSE
)
}
\arguments{
\item{digest_result}{A digest tibble produced by \code{\link[=digest_protein]{digest_protein()}} or an
equivalent table containing the columns \code{protein_id}, \code{peptide}, \code{start},
\code{end}, \code{length}, and \code{missed_cleavages}. If \code{NULL} or missing required
columns, raises an error.}

\item{proteome}{Optional digest tibble representing the comparison proteome
used for peptide uniqueness scoring. When \code{NULL} (default), \code{S_unique} is
excluded and protein-only default weights are used.}

\item{weights}{Optional numeric weight vector. In protein-only mode the
default weights are \code{c(S_length = 0.200, S_coverage = 0.348, S_count = 0.226, S_hydro = 0.138, S_charge = 0.088)}.
In proteome-aware mode the weights are
\code{c(S_length = 0.160, S_coverage = 0.279, S_count = 0.181, S_hydro = 0.110, S_charge = 0.070, S_unique = 0.200)}.
Weights were derived via analytical hierarchy process (AHP) with
pairwise comparisons grounded in proteomics literature: peptide length
is the primary MS detectability filter, sequence coverage carries the
main biological signal, peptide count supports statistical confidence,
and GRAVY/charge capture independent LC-MS dimensions.}

\item{gravy_range}{Numeric vector of length 2 defining the inclusive GRAVY
range used by \code{S_hydro}. Defaults to \code{c(-1.0, 0.6)}. If \code{NULL}, raises an
error.}

\item{length_range}{Integer vector of length 2 defining the inclusive valid
peptide length range used by \code{S_length}, \code{S_coverage}, \code{S_count},
\code{S_hydro}, \code{S_charge}, and \code{S_unique}. Defaults to \code{c(7L, 25L)}. If
\code{NULL}, raises an error.}

\item{enzyme}{Cleavage rule name used to choose the fallback expected peptide
length when the digest contains fewer than three peptides. Defaults to
\code{"trypsin"}. If \code{NULL}, raises an error.}

\item{include_pI}{Logical flag indicating whether to append a \code{pI} list
column containing peptide-level pI values for valid peptides. Defaults to
\code{FALSE}. If \code{NULL}, raises an error.}
}
\value{
A tibble with one row per \code{protein_id} and the component score
columns \code{S_length}, \code{S_coverage}, \code{S_count}, \code{S_hydro},
\code{S_charge},
optional \code{S_unique}, plus \code{composite_score}, \code{verdict}, and
\code{median_peptide_length}, and \code{preset_used}. The
\code{median_peptide_length} column records the digest-level denominator used in
the enzyme-aware \code{S_count} calculation. The \code{preset_used} column records
the named preset whose resolved scoring configuration exactly matches the
current call, or \code{"custom"} otherwise. When \code{include_pI = TRUE}, the
output also includes a \code{pI} list column with one named numeric vector per
protein, storing valid-peptide pI values keyed by peptide sequence.
}
\description{
\code{score_peptides()} summarizes a pepVet digest tibble into per-protein scoring
components and a weighted composite suitability score. The scoring model is
designed for digest planning and enzyme comparison, not for post-search
peptide detectability prediction.
}
\details{
Valid peptides are defined as peptides with lengths between 7 and
25 residues inclusive by default, but this window can be changed with
\code{length_range}. Coverage is computed from valid peptide coordinates with
overlapping intervals reduced before coverage is summed. \code{S_hydro} uses
the inclusive \code{gravy_range}; mixed sequences average known hydrophobicity
values, and sequences with no known values score zero. \code{S_unique} is only
computed when a proteome digest is supplied. \code{S_charge} measures the
fraction of valid peptides that contain at least one non-terminal basic
residue to capture extra charge-state richness rather than baseline
ionizability.
Composite verdicts are classified as \code{Good} for scores >= 0.65,
\code{Moderate} for scores >= 0.4, and \code{Poor} otherwise.
}
\section{Limitations}{

pepVet is a rule-based digest-ranking model, not a peptide detectability
predictor. The verdict thresholds are heuristic, the default and preset
weights are expert priors rather than empirically fit coefficients, and the
scoring windows assume conventional C18 reversed-phase LC with ESI. pepVet
does not model PTMs, chemical labels, chromatographic gradients, or
instrument-specific fragmentation behavior. Composite scores are
interpretable rankings with no physical unit; they are not calibrated
probabilities. Cross-workflow comparisons are only meaningful when the
resolved scoring configuration matches, which is why \code{preset_used} is
recorded in the output.
}

\examples{
digest_result <- digest_protein("MKWVTFISLLFLFSSAYSR")
score_peptides(digest_result)

target_and_background <- digest_protein(
  c(target = "AAAAAAARAAAAAAAK", background = "AAAAAAARGGGGGGGK")
)
score_peptides(
  target_and_background[target_and_background$protein_id == "target", ],
  proteome = target_and_background
)
}
\concept{scoring}
