% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{triage_proteins}
\alias{triage_proteins}
\title{Triage proteins from a batch evaluation}
\usage{
triage_proteins(batch_result)
}
\arguments{
\item{batch_result}{A tibble returned by \code{\link[=batch_evaluate]{batch_evaluate()}}. If \code{NULL} or
empty, raises an error.}
}
\value{
A tibble with one row per protein containing all score and flag
columns from the flat batch summary, plus an \code{action} column. Possible
values:
\describe{
\item{\code{"proceed"}}{Good verdict. No intervention indicated.}
\item{\code{"consider_alternative"}}{Moderate verdict without a
sequence-level difficulty flag. Review component scores and consider a
preset or missed-cleavage adjustment.}
\item{\code{"try_other_enzyme"}}{Moderate or Poor verdict with
\code{flag_hydrophobic} or \code{flag_short_protein}, or any Poor
verdict without an intrinsic complexity flag. An alternative enzyme
is the most likely improvement path.}
\item{\code{"skip"}}{No valid peptides or low-complexity sequence. No
standard enzyme choice is expected to substantially improve the
score.}
}
}
\description{
\code{triage_proteins()} appends an \code{action} column to the flat tibble returned
by \code{\link[=batch_evaluate]{batch_evaluate()}} with deterministic recommendations based on each
protein's verdict and difficulty flags.
}
\section{Limitations}{

Triage actions are advisory, based on heuristic difficulty flags from the
batch score columns. They do not re-evaluate the protein with alternative
enzymes. Use \code{\link[=compare_digests]{compare_digests()}} for that.
}

\examples{
small_path <- system.file(
  "extdata", "small_proteome_50_proteins.fasta",
  package = "pepVet"
)
batch <- batch_evaluate(small_path, enzyme = "trypsin")
triaged <- triage_proteins(batch)
table(triaged$action)
}
\seealso{
\code{\link[=batch_evaluate]{batch_evaluate()}}, \code{\link[=summarize_batch]{summarize_batch()}}

Other evaluation:
\code{\link[=batch_compare_enzymes]{batch_compare_enzymes()}},
\code{\link[=batch_evaluate]{batch_evaluate()}},
\code{\link[=compare_digests]{compare_digests()}},
\code{\link[=evaluate_digest]{evaluate_digest()}},
\code{\link[=recommend_enzyme]{recommend_enzyme()}},
\code{\link[=sensitivity_analysis]{sensitivity_analysis()}},
\code{\link[=summarize_batch]{summarize_batch()}}
}
\concept{evaluation}
